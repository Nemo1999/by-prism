# 歌曲製作指南

## 概述
本指南提供完整的日文歌曲學習內容製作流程，從新歌登記到內容擴展的完整步驟。

## 核心原則

### 優先級順序
1. **發音按鈕完整性**（最高優先級）：所有單字和句子必須有發音按鈕
2. **拼音標註正確性**（高優先級）：只標註漢字，非漢字不標註
3. **雙向連結完整性**（高優先級）：建立完整的網狀連結結構
4. **新內容創建**（中優先級）：在現有內容優化後進行

### 工作流程
```
優先優化現有內容 → 再創建新內容
```

## 歌曲製作三階段

### 階段一：新歌登記與初始化
**目標**：建立新歌曲的基礎結構

#### 步驟：
1. **創建歌曲專案目錄**
   ```
   src/content/posts/japanese-learning/songs/{song-name}/
   ```

2. **創建必要頁面**
   - `index.md` - 歌曲學習專案入口頁面
   - `full-lyrics.md` - 完整歌詞頁面
   - `study-progress.md` - 學習進度頁面
   - `vocabulary-grammar-review.md` - 單字文法複習頁面

3. **更新系統狀態**
   - 更新 `.song-focus-state.json` 設定當前焦點歌曲
   - 更新 `digital-garden-index.md` 登記新歌

4. **設定初始模板**
   - 使用標準模板創建頁面
   - 設定初始學習進度
   - 建立基本連結結構

### 階段二：歌詞解析擴展
**目標**：逐步解析歌曲的每一句歌詞

#### 步驟：
1. **檢查當前焦點歌曲**
   ```bash
   # 讀取當前焦點
   cat .song-focus-state.json | jq '.currentSong.name'
   ```

2. **選擇下一句歌詞**
   - 根據學習進度選擇
   - 考慮學習難度和重要性
   - 確保連續性

3. **創建歌詞解析頁面**
   - 使用 `lyrics-analysis.md` 模板
   - 檔案命名：`{song-name}-{line-description}.md`
   - 位置：`songs/{song-name}/lyrics/`

4. **添加必要元素**
   - 拼音標註（只標註漢字）
   - 發音按鈕（所有單字和句子）
   - 中文翻譯和羅馬拼音
   - 文法解析和單字說明

5. **建立雙向連結**
   ```markdown
   ## 相關連結
   - 上一句：[[{previous-line-filename}|{previous-line-title}]]
   - 下一句：[[{next-line-filename}|{next-line-title}]]
   - 完整歌詞：[[{song-name}-full-lyrics|{song-name}完整歌詞]]
   - 學習專案：[[{song-name}/index|《{song-name}》學習專案]]
   
   ## 單字連結
   - [[vocabulary/{word1}|{word1-zh}]]
   - [[vocabulary/{word2}|{word2-zh}]]
   
   ## 文法連結
   - [[grammar/{grammar1}|{grammar1-zh}]]
   - [[grammar/{grammar2}|{grammar2-zh}]]
   ```

6. **更新相關頁面**
   - 更新完整歌詞頁面，添加新句子的連結
   - 更新學習進度頁面，記錄進度
   - 更新單字文法複習頁面，添加新內容
   - 更新系統狀態，記錄當前進度

### 階段三：內容品質檢查與連結完善
**目標**：確保內容品質和連結完整性

#### 檢查項目：
1. **發音按鈕完整性檢查**
   - 所有單字都有發音按鈕
   - 所有句子都有發音按鈕
   - 按鈕功能正常運作
   - 按鈕位置適當易用

2. **拼音標註正確性檢查**
   - 只標註漢字，非漢字不標註
   - 使用正確的 `<ruby>漢字<rt>ふりがな</rt></ruby>` 格式
   - 避免重複標註
   - 標註與發音按鈕協調一致

3. **雙向連結完整性檢查**
   - 歌詞頁面連結到單字頁面
   - 單字頁面連結回歌詞頁面
   - 歌詞頁面連結到文法頁面
   - 文法頁面連結回歌詞頁面
   - 上下文連結（上一句↔下一句）

4. **避免重複連結檢查**
   - 檢查頁面內是否有重複連結
   - 合併或刪除重複連結
   - 確保每個連結都有意義

5. **連結目標存在性檢查**
   - 檢查所有連結目標頁面是否存在
   - 修正錯誤的連結路徑
   - 補充缺失的目標頁面

## Cron Job 整合

### 現有 Cron Jobs 更新

#### 1. 每日內容擴展
**新增功能**：新歌登記功能整合
```yaml
檢查項目：
- 檢查是否需要開始新歌
- 讀取系統狀態決定下一步
- 根據優先級決定優化或創建
- 執行階段二或階段三任務
```

#### 2. 內容品質檢查
**增強功能**：系統化檢查流程
```yaml
檢查項目：
- 讀取當前焦點歌曲
- 根據系統設定決定檢查優先級
- 執行階段三的品質檢查
- 記錄檢查結果和修正
```

#### 3. 快速內容填充
**專注功能**：小步快跑優化
```yaml
檢查項目：
- 優先補充缺失發音按鈕
- 優先修正錯誤拼音標註
- 優先建立缺失雙向連結
- 每次任務修正幾個問題
```

### Cron Job 協同工作流程
```
每日內容擴展（02:00） → 決定優化或創建
    ↓
內容品質檢查（00:00） → 系統化品質檢查
    ↓
快速內容填充（00:30,01:30,02:30,03:30,04:30） → 小步優化
```

## 系統狀態管理

### 狀態檔案：`.song-focus-state.json`
```json
{
  "currentSong": {
    "name": "歌曲名稱",
    "directory": "路徑",
    "progress": {
      "totalLines": "總句數",
      "completedLines": "完成句數",
      "currentLine": "當前句數"
    },
    "priority": "當前優先級"
  },
  "systemSettings": {
    "priorityOrder": ["發音按鈕", "拼音標註", "雙向連結", "新內容"],
    "optimizationFirst": true
  }
}
```

### 狀態讀取與更新
```bash
# 讀取當前焦點歌曲
CURRENT_SONG=$(jq -r '.currentSong.name' .song-focus-state.json)

# 讀取優先級設定
PRIORITY_ORDER=$(jq -r '.systemSettings.priorityOrder[]' .song-focus-state.json)

# 更新學習進度
jq '.currentSong.progress.completedLines += 1' .song-focus-state.json > temp.json && mv temp.json .song-focus-state.json
```

## 模板檔案

### 1. 歌曲專案入口模板
位置：`_templates/song-project-index.md`
用途：新歌學習專案的入口頁面

### 2. 歌詞解析模板
位置：`_templates/lyrics-analysis.md`
用途：單句歌詞的解析頁面

### 3. 完整歌詞模板
位置：`_templates/song-full-lyrics.md`
用途：歌曲完整歌詞頁面

### 4. 學習進度模板
位置：`_templates/study-progress.md`
用途：追蹤學習進度的頁面

### 5. 單字文法複習模板
位置：`_templates/vocabulary-grammar-review.md`
用途：單字和文法總複習頁面

## 檢查命令參考

### 發音按鈕檢查
```bash
# 檢查日文內容是否缺少發音按鈕
grep -r "[一-龯ぁ-んァ-ン]" src/content --include="*.md" | grep -v "pronunciation-button\|發音按鈕\|play-button" | head -10
```

### 拼音標註檢查
```bash
# 檢查非漢字有拼音標註的錯誤
grep -r "<ruby>[^<]*<rt>[^<]*</rt></ruby>" src/content --include="*.md" | grep -v "[一-龯]" | head -5
```

### 連結完整性檢查
```bash
# 檢查歌詞頁面的上下文連結
find src/content -name "*.md" -type f -exec grep -l "Lemon" {} \; | xargs -I {} sh -c 'echo "檢查: {}"; grep -q "上一句\|下一句\|整首歌" {} && echo "  ✓ 有上下文連結" || echo "  ✗ 缺少上下文連結"'
```

### 重複連結檢查
```bash
# 檢查頁面內的重複連結
find src/content -name "*.md" -type f -exec sh -c 'echo "檢查: $1"; grep -o "\[\[.*\]\]" "$1" | sort | uniq -d | head -5' _ {} \; | grep -v "^檢查:"
```

## 品質標準

### 發音按鈕標準
- ✅ 所有日文單字都有發音按鈕
- ✅ 所有日文句子都有發音按鈕
- ✅ 按鈕功能正常，點擊後播放發音
- ✅ 按鈕設計一致，位置適當
- ✅ 發音品質清晰準確

### 拼音標註標準
- ✅ 只標註漢字，非漢字不標註
- ✅ 使用正確的 `<ruby>漢字<rt>ふりがな</rt></ruby>` 格式
- ✅ 避免重複標註（正文已有假名的不重複）
- ✅ 標註與發音按鈕協調一致

### 連結完整性標準
- ✅ 歌詞頁面有完整的上下文連結
- ✅ 單字頁面連結到所有使用此單字的歌詞
- ✅ 文法頁面連結到所有使用此文法的歌詞
- ✅ 所有連結都是雙向的
- ✅ 沒有重複或無意義的連結

## 故障排除

### 常見問題
1. **發音按鈕不工作**
   - 檢查 JavaScript 是否正確載入
   - 檢查發音資料是否存在
   - 測試按鈕功能

2. **拼音標註顯示錯誤**
   - 檢查 HTML 語法是否正確
   - 確認只標註漢字
   - 避免標註重複

3. **連結失效**
   - 檢查連結目標頁面是否存在
   - 確認連結路徑是否正確
   - 更新錯誤的連結

4. **系統狀態不同步**
   - 檢查 `.song-focus-state.json` 格式
   - 確認所有 cron jobs 使用相同狀態檔案
   - 定期備份狀態檔案

### 維護建議
1. **定期備份**：備份系統狀態和重要頁面
2. **監控日誌**：記錄 cron jobs 的執行結果
3. **定期審核**：每週審核內容品質
4. **持續優化**：根據使用者反饋持續改進

## 更新記錄

### 2026-03-22
- 建立完整的歌曲製作指南
- 整合新歌登記功能到現有 cron jobs
- 建立系統狀態管理機制
- 設定優先優化再創建的工作流程

### 未來計劃
- 添加更多歌曲製作模板
- 建立自動化測試系統
- 擴展到多首歌曲同時管理
- 添加使用者學習追蹤功能

---

**最後更新**：2026年3月22日  
**版本**：1.0  
**適用範圍**：日文學習數位花園歌曲製作  
**維護者**：小波（日文學習數位花園管理者）